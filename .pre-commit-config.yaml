repos:
  - repo: local
    hooks:
      - id: sync-root-assets
        name: Sync root assets (local)
        entry: python -m tools.sync_root
        language: system
        stages: [pre-commit, pre-push]
